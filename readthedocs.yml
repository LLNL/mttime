version:  2

conda:
    environment: docs/environment.yml

python:
  version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
          - rtd

sphinx:
  builder: html
  fail_on_warning: false